/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ['en', 'kn', 'hi', 'te', 'ta'], // English (en), Kannada (kn), Hindi (hi), Telugu (te), Tamil (ta)
      defaultLocale: 'en', // Default language is English
    },
  };
  
  export default nextConfig;
  